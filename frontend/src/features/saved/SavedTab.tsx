// Tab "Từ đã lưu" + practice popup + review-toast — port ở M4.
export default function SavedTab() {
  return (
    <div id="mode-saved">
      <div className="card">
        <h2>📚 Từ đã lưu</h2>
        <p className="mode-note">Tab này sẽ được port ở M4 (saved words + practice popup + review-toast).</p>
      </div>
    </div>
  );
}
